export default class Tshirt {
    constructor(
        // define parameters
        name,
        color,
        size
    ) {
        // define properties
        this.name = name,
        this.color = color,
        this.size = size
    }
}

